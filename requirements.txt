# Python runtime dependencies. AMRFinderPlus is native software and is installed
# through environment.yml on Streamlit Community Cloud.
.
streamlit==1.59.1
supabase==2.31.0
python-dotenv
pypdf
# Optional: enables the flag-gated plain-language clinician summary. Inactive
# unless OPENAI_API_KEY is set — the app runs fine without it.
openai>=1.0
